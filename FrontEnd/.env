# VITE_API_BASE_URL is not set for local dev - uses /api default which goes through Vite proxy
# This allows testing from other devices on the network (e.g., mobile phones)

# Use hash routing for local development (matches GitHub Pages behavior)
VITE_USE_HASH_ROUTING=true
